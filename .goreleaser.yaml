version: 2

project_name: omnis

before:
  hooks:
    - go mod tidy

builds:
  - id: omnis
    binary: omnis
    main: .
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}
      - -X main.commit={{ .Commit }}
      - -X main.date={{ .Date }}
    # Windows builds rely on the platform-guarded shell helpers in
    # core/tools/bash_unix.go / bash_windows.go (the Unix-only Setpgid/Kill
    # path is no longer compiled into the Windows binary).
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

  - id: omnis-server
    binary: omnis-server
    main: ./server
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}
      - -X main.commit={{ .Commit }}
      - -X main.date={{ .Date }}
    # Windows builds rely on the platform-guarded shell helpers in
    # core/tools/bash_unix.go / bash_windows.go (the Unix-only Setpgid/Kill
    # path is no longer compiled into the Windows binary).
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: default
    ids:
      - omnis
      - omnis-server
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
    formats:
      - tar.gz
    # Windows users expect a .zip rather than a tar.gz; this is also the
    # artifact the WiX MSI pipeline unpacks to source the binaries + assets.
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - LICENSE
      - README.md
      - src: docs
        dst: docs
      - src: config/*.json
        dst: config
      - src: config/server.yaml
        dst: config/server.yaml
      - src: config/filters/*.json
        dst: config/filters
      - src: registry/skills
        dst: registry/skills
      - src: registry/agents
        dst: registry/agents
      - src: web
        dst: web

nfpms:
  - id: linux-packages
    package_name: omnis
    ids:
      - omnis
      - omnis-server
    file_name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "arm64" }}aarch64
      {{- else }}{{ .Arch }}{{ end }}
    vendor: Bertrand Louargant
    homepage: https://github.com/blouargant/omnis
    maintainer: Bertrand Louargant <blouargant@chapsvision.fr>
    description: |-
      Omnis - multi-agent harness providing a CLI/TUI/REPL (omnis) and an
      HTTP API + Web UI server (omnis-server). The same binary becomes a
      code reviewer, Kubernetes triage assistant, or DBA helper purely by
      mounting different tools, skills, and MCP servers.
    license: MIT
    formats:
      - deb
      - rpm
    bindir: /usr/bin
    section: utils
    priority: optional
    rpm:
      group: Applications/System

    contents:
      # Editable configuration (preserved on upgrade).
      # Config files install under /etc/omnis/ — the system layer of the
      # 3-tier search chain (.agents > $HOME/.omnis > /etc/omnis). The
      # agent/skill registries live under /etc/omnis/registry/ (see below).
      - src: config/agents.json
        dst: /etc/omnis/agents.json
        type: "config|noreplace"
        file_info:
          mode: 0644
      - src: config/models.json
        dst: /etc/omnis/models.json
        type: "config|noreplace"
        file_info:
          mode: 0644
      - src: config/mcp_config.json
        dst: /etc/omnis/mcp_config.json
        type: "config|noreplace"
        file_info:
          mode: 0644
      - src: config/permissions.json
        dst: /etc/omnis/permissions.json
        type: "config|noreplace"
        file_info:
          mode: 0644
      - src: config/preferences.json
        dst: /etc/omnis/preferences.json
        type: "config|noreplace"
        file_info:
          mode: 0644
      - src: config/remote_registries.json
        dst: /etc/omnis/remote_registries.json
        type: "config|noreplace"
        file_info:
          mode: 0644
      - src: packaging/etc-omnis/server.yaml
        dst: /etc/omnis/server.yaml
        type: "config|noreplace"
        file_info:
          mode: 0644
      - src: config/a2a_config.json
        dst: /etc/omnis/a2a_config.json
        type: "config|noreplace"
        file_info:
          mode: 0644
      - src: config/filters/*.json
        dst: /etc/omnis/filters/
        type: "config|noreplace"
        file_info:
          mode: 0644

      # Login-shell env defaults (OMNIS_CONFIG_PATH, OMNIS_WEB_DIR).
      - src: packaging/profile.d/omnis.sh
        dst: /etc/profile.d/omnis.sh
        file_info:
          mode: 0644

      # Bundled agent definitions, skill playbooks and Web UI assets (read-only).
      # `type: tree` preserves the directory structure under src.
      - src: registry/agents
        dst: /etc/omnis/registry/agents
        type: tree
      - src: registry/skills
        dst:  /etc/omnis/registry/skills
        type: tree
      - src: web
        dst: /usr/share/omnis/web
        type: tree

      # Mutable curator output — owned by root but world-writable so any
      # local user running `omnis` can let the curator persist soft-skills.
      - dst: /var/lib/omnis/softskills
        type: dir
        file_info:
          mode: 0777

      # Documentation.
      - src: LICENSE
        dst: /usr/share/doc/omnis/LICENSE
        file_info:
          mode: 0644
      - src: README.md
        dst: /usr/share/doc/omnis/README.md
        file_info:
          mode: 0644
      - src: docs
        dst: /usr/share/doc/omnis/docs
        type: tree

    overrides:
      rpm:
        dependencies: []
        recommends: []
      deb:
        dependencies: []
        recommends: []

brews:
  - name: omnis
    # Source the macOS/Linux archive (binaries + bundled config/web/registry).
    ids:
      - default
    repository:
      owner: blouargant
      name: homebrew-tap
      branch: main
      # PAT with write access to blouargant/homebrew-tap, exposed in CI.
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/blouargant/omnis"
    description: "Multi-agent harness: CLI/TUI (omnis) + HTTP API & Web UI server (omnis-server)"
    license: "MIT"
    # Never publish a formula from rc/beta pre-release tags.
    skip_upload: auto
    install: |
      # Keep the real binaries in libexec; expose env-injecting wrappers on
      # PATH so omnis finds the bundled web assets + config without the user
      # setting anything. OMNIS_SYSTEM_CONFIG_DIR relocates only the system
      # layer of the config search chain, leaving ./.agents and ~/.omnis intact.
      libexec.install "omnis", "omnis-server"
      (bin/"omnis").write_env_script libexec/"omnis",
        OMNIS_WEB_DIR:           pkgshare/"web",
        OMNIS_SYSTEM_CONFIG_DIR: pkgshare
      (bin/"omnis-server").write_env_script libexec/"omnis-server",
        OMNIS_WEB_DIR:           pkgshare/"web",
        OMNIS_SYSTEM_CONFIG_DIR: pkgshare

      # Bundled defaults, refreshed on every `brew upgrade`. User overrides
      # live in ~/.omnis (higher precedence) so upgrades never clobber them.
      pkgshare.install "web"
      pkgshare.install "registry"
      pkgshare.install Dir["config/*.json"]
      pkgshare.install "config/server.yaml"
      (pkgshare/"filters").install Dir["config/filters/*.json"]
    caveats: |
      Bundled agents, skills and the Web UI install under
        #{HOMEBREW_PREFIX}/share/omnis
      and are refreshed on every `brew upgrade`. Put your own overrides in
        ~/.omnis   (e.g. ~/.omnis/agents.json, ~/.omnis/models.json)
      which take precedence over the bundled defaults.

      Set a provider API key before first run, e.g.:
        export ANTHROPIC_API_KEY=sk-...

      Run the Web UI / HTTP API server (listens on :8080, unauthenticated by
      default — set OMNIS_SERVER_TOKEN to require a bearer token):
        omnis-server
      or as a background service:
        brew services start omnis
    service: |
      run [opt_bin/"omnis-server"]
      keep_alive true
      log_path var/"log/omnis-server.log"
      error_log_path var/"log/omnis-server.log"
      working_dir var
    test: |
      system bin/"omnis", "version"

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-snapshot"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  # Stable tags (vA.B.C) publish as a normal GitHub release; rc/beta tags
  # (vA.B.C-rcN, vA.B.C-betaN) are valid semver pre-releases, so
  # `prerelease: auto` flips the GitHub pre-release flag automatically.
  draft: false
  prerelease: auto
  # Make re-runs idempotent: if a release for the tag already exists (e.g. a
  # previous workflow run uploaded some/all assets, or the run was retried),
  # delete any same-named asset before re-uploading instead of failing with
  # `422 ... already_exists`.
  replace_existing_artifacts: true
