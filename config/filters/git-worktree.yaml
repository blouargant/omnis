name: "git-worktree"
version: 1
description: "Condensed git worktree output"

match:
  command: "git"
  subcommand: "worktree"

pipeline:
  - action: "strip_ansi"
  - action: "keep_lines"
    pattern: "\\S"
  - action: "head"
    n: 20

on_error: "passthrough"
