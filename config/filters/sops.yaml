name: "sops"
version: 1
description: "Condensed sops output"

match:
  command: "sops"

streams:
  - stdout
  - stderr

pipeline:
  - action: "strip_ansi"
  - action: "truncate_lines"
    max: 120
  - action: "head"
    n: 30

on_error: "passthrough"
